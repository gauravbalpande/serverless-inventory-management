{
  "name": "cloud-inventory-backend",
  "version": "1.0.0",
  "description": "Serverless backend (Lambda + API Gateway + DynamoDB + SNS) for the cloud-based inventory management system.",
  "main": "src/inventoryHandler.js",
  "scripts": {
    "lint": "echo \"No linter configured\"",
    "test": "node src/inventoryHandler.test.js || echo \"No tests yet\""
  },
  "dependencies": {
    "uuid": "^9.0.1",
    "aws-sdk": "^2.1691.0"
  },
  "devDependencies": {}
}



